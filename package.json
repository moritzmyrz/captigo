{
  "name": "captigo",
  "private": true,
  "version": "0.0.0",
  "description": "Provider-agnostic CAPTCHA library ecosystem for the modern web",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter captigo --filter @captigo/shared run build && pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.4"
}
