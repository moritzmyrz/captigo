// Ambient declarations — picked up automatically by TypeScript for all files in src/.

interface Window {
  // eslint-disable-next-line @typescript-eslint/consistent-type-definitions
  turnstile?: import("./types.js").TurnstileSDK;
}
